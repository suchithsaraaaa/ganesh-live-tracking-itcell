<?xml version="1.0" encoding="utf-8"?>
<!-- Backups are disabled (android:allowBackup="false") for this operational app —
     session tokens and queued telemetry must never leave the device via cloud backup. -->
<full-backup-content>
</full-backup-content>
